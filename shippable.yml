language: java

jdk:
        - oraclejdk8

build:
        ci:
                - mkdir -p shippable/testresults
                - mkdir -p shippable/codecoverage

                - shippable_retry mvn clean install
